import { SpinnerContainer } from "../../src/styles/Loading.styles";

const Loading = () => {
  return (
    <SpinnerContainer>
      <div className="spinner" />
    </SpinnerContainer>
  );
};

export default Loading;
